import PlacementsClient from './PlacementsClient'

export const metadata = {
  title: 'Placements',
  description: 'Nettms Education placement success stories. 85% placement rate. Students placed at TCS, Infosys, Wipro, HCL and more.',
}

export default function PlacementsPage() {
  return <PlacementsClient />
}